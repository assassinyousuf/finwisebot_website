import AnimatedLoginForm from '../components/AnimatedLoginForm';

export default function Login() {
  // Keep /login route but render the animated form for a more polished look.
  return <AnimatedLoginForm />;
}
