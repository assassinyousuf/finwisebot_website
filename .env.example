# Copy this file to .env.local and fill the values (DO NOT COMMIT .env.local)
# MongoDB Atlas connection string (replace <user> and <password>)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/finwisebot?retryWrites=true&w=majority
MONGODB_DB=finwisebot

# JWT secret for signing tokens (set a long random value)
JWT_SECRET=replace_with_a_long_random_string

# Optional: SMTP or SendGrid creds for email verification
#SENDGRID_API_KEY=
#SMTP_HOST=
#SMTP_USER=
#SMTP_PASS=
